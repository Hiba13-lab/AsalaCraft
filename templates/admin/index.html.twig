{% extends 'base.html.twig' %}

{% block title %}Panneau d'administration{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1 class="text-danger">🛡️ Dashboard Administrateur</h1>
    <hr>

    <div class="row mt-4">
        {# Carte Utilisateurs #}
        <div class="col-md-4 mb-3">
            <div class="card bg-light p-3 border-primary shadow-sm h-100">
                <div class="d-flex justify-content-between">
                    <h3>👥 Utilisateurs</h3>
                    <span class="badge bg-primary fs-5">{{ countUsers }}</span>
                </div>
                <p>Gérer les clients, les vendeurs et leurs rôles.</p>
                <a href="{{ path('admin_users') }}" class="btn btn-primary mt-auto w-100">Accéder à la gestion</a>
            </div>
        </div>

        {# Carte Produits (MAINTENANT FONCTIONNELLE) #}
        <div class="col-md-4 mb-3">
            <div class="card bg-light p-3 border-success shadow-sm h-100">
                <div class="d-flex justify-content-between">
                    <h3>🛒 Produits</h3>
                    <span class="badge bg-success fs-5">{{ countProducts }}</span>
                </div>
                <p>Modérer les annonces et supprimer les produits signalés.</p>
                <a href="{{ path('admin_products') }}" class="btn btn-success mt-auto w-100">Modérer les produits</a>
            </div>
        </div>

        {# Carte Statistiques #}
        <div class="col-md-4 mb-3">
            <div class="card bg-light p-3 shadow-sm h-100 border-secondary">
                <h3>📊 Statistiques</h3>
                <p>Volume des ventes et revenus de la plateforme.</p>
                <button class="btn btn-secondary disabled mt-auto w-100">Bientôt disponible</button>
            </div>
        </div>
    </div>
</div>
{% endblock %}