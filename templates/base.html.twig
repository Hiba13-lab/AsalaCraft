<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}AsalaCraft{% endblock %}</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    </head>
    <body class="bg-light">
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4 shadow">
            <div class="container">
                <a class="navbar-brand fw-bold" href="{{ path('app_home') }}">🏺 AsalaCraft</a>

                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>
                
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav me-auto">
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_home') }}">Boutique</a>
                        </li>

                        {# MENU ARTISAN : Visible uniquement pour ceux qui ont un profil vendeur #}
                        {% if app.user and app.user.sellerProfile %}
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle text-warning fw-bold" href="#" id="artisanMenu" role="button" data-bs-toggle="dropdown">
                                    🛠️ Espace Artisan
                                </a>
                                <ul class="dropdown-menu shadow">
                                    <li><a class="dropdown-item" href="{{ path('app_product_index') }}">📦 Mes Produits</a></li>
                                    <li><a class="dropdown-item" href="{{ path('app_seller_orders') }}">📋 Commandes reçues</a></li>
                                    <li><hr class="dropdown-divider"></li>
                                    <li><a class="dropdown-item text-primary" href="{{ path('app_product_new') }}">+ Ajouter un produit</a></li>
                                </ul>
                            </li>
                        {% endif %}

                        {# LIEN ADMIN #}
                        {% if is_granted('ROLE_ADMIN') %}
                            <li class="nav-item border-start ms-2 ps-2">
                                <a class="nav-link text-danger fw-bold" href="{{ path('admin_dashboard') }}">🛡️ Administration</a>
                            </li>
                        {% endif %}
                    </ul>

                    <ul class="navbar-nav ms-auto align-items-center">
                        {# PANIER (caché pour les vendeurs pour éviter les erreurs) #}
                        {% if not (app.user and app.user.sellerProfile) %}
                            <li class="nav-item me-3">
                                <a class="nav-link btn btn-outline-info text-light px-3 rounded-pill btn-sm" href="{{ path('app_cart_index') }}">
                                    🛒 Panier
                                </a>
                            </li>
                        {% endif %}

                        {% if app.user %}
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle text-light small" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown">
                                    Connecté : {{ app.user.email }}
                                </a>
                                <ul class="dropdown-menu dropdown-menu-end shadow">
                                    <li><span class="dropdown-item-text small text-muted">Profil : {{ app.user.profileName }}</span></li>
                                    <li><hr class="dropdown-divider"></li>
                                    <li><a class="dropdown-item text-danger" href="{{ path('app_logout') }}">Déconnexion</a></li>
                                </ul>
                            </li>
                        {% else %}
                            <li class="nav-item"><a class="nav-link" href="{{ path('app_login') }}">Connexion</a></li>
                            <li class="nav-item"><a class="btn btn-primary btn-sm ms-2" href="{{ path('app_register') }}">S'inscrire</a></li>
                        {% endif %}
                    </ul>
                </div>
            </div>
        </nav>

        <main class="container">
            {% for label, messages in app.flashes %}
                {% for message in messages %}
                    <div class="alert alert-{{ label == 'error' or label == 'danger' ? 'danger' : 'success' }} alert-dismissible fade show shadow-sm border-0">
                        {{ message }}
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                {% endfor %}
            {% endfor %}

            {% block body %}{% endblock %}
        </main>

        <footer class="text-center py-4 mt-5 text-muted small">
            &copy; {{ "now"|date("Y") }} AsalaCraft - Espace Artisanal
        </footer>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    </body>
</html>